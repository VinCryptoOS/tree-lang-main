namespace utils;
public static class DateTimeStrings
{
    public static string getTimeString(DateTime time, bool includeDate = true, bool includeTime = true)
    {
        return "";
    }
}
